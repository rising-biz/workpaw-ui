# Task 1.4 Report: useFetch hook + build verification

## Status: DONE

## What was done

1. **Created `src/hooks/useFetch.ts`** -- generic `useFetch<T>` hook with `data`, `error`, `loading`, and `refetch` fields; supports `initialData` and `immediate` options.
2. **Verified `src/lib/utils.ts`** -- already present with the `cn` (clsx + tailwind-merge) utility.
3. **Updated `src/index.ts`** -- added `export { useFetch } from "./hooks/useFetch"` and `export { cn } from "./lib/utils"`.
4. **Added `tsconfig.tsbuildinfo` to `.gitignore`** -- this file is generated by `tsc -b` and should not be committed.

## Verification

- `npx tsc --noEmit`: PASSED (no errors).
- `npm run build`: PASSED. Output `dist/index.js` (40.34 kB, 10.77 kB gzip), 13 modules transformed. Both `useFetch` and `cn` are present in the built artifact.

## Commits

- `bb1a557` feat: add useFetch hook, verify build

## Files changed

- `src/hooks/useFetch.ts` (new)
- `src/index.ts` (modified -- added hook and lib exports)
- `.gitignore` (modified -- added `tsconfig.tsbuildinfo`)

## Concerns

None. The `cn` function was already defined in `src/lib/utils.ts` but was not previously re-exported from `src/index.ts`. It is now properly exported for library consumers.
